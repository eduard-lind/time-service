{
  "name": "time-service",
  "version": "1.0.0",
  "description": "A simple REST service which tells time",
  "main": "server.js",
  "scripts": {
    "lint": "eslint *.js",
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4"
  },
  "devDependencies": {
    "concurrently": "^2.1.0",
    "eslint": "^2.11.1",
    "eslint-config-standard": "^5.3.1",
    "nodemon": "^1.9.2"
  }
}
